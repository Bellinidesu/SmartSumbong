<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Android 13+ (API 33) requires this explicit runtime grant before
         ANY notification shows, including FCM push and local ones. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Android 13+ (API 33) granular media permissions. The system
         Photo Picker that image_picker uses for gallery source needs
         neither of these — it grants access per-item without a broad
         permission at all — so on a modern handset requesting these
         resolves as already-granted and PermissionGate's dialog never
         actually shows. They exist for the two things that still need a
         real runtime grant: capturing evidence with the camera on any
         version, and reading the gallery directly on Android 12 and
         below via the legacy permission below. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <!-- local_auth (Settings' Face ID / fingerprint unlock toggle) --
         androidx.biometric's own required permission for reading the
         device's enrolled biometrics through BiometricPrompt. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <application
        android:label="SmartSumbong Resident"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Push notification icon + accent colour (27 Aug 2026), from a
             reference the user sent: the small circle Android draws next
             to a notification in the shade (Gmail's blue circle, Play
             services' pink circle, each app's own colour behind a
             plain glyph). Without these two lines FCM falls back to the
             launcher icon rendered as a plain white square, which is
             what the app was actually showing before this.

             ic_stat_notify (res/drawable-*dpi/) is a solid bell
             silhouette; Android discards an icon's colour here and
             keeps only its alpha channel, tinting the shape itself
             white regardless of what colour the source PNG is drawn
             in, so a full-colour illustration would not survive this
             the way it does everywhere else in the app. The circle
             behind it is notification_color (res/values/
             notification_colors.xml), this app's orange accent,
             not the resident navy, so it doesn't just read as a solid
             navy blob against a dark shade the way the in-app bell can. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
    <queries>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
    </queries>
</manifest>
